# Session — 2026-06-08

**Ideas touched**: [[equipment-finance-broker-first]] (new), [[autonomous-machine-operating-model]] (the "anchor a build to a live consumer" move reused), [[ui-surface-infra]] (one-primitive-multiple-motions)

## What Was Explored

A long, three-arc session on equipment finance. (1) *If Stauss dropped off, what's the GTM?* → **broker-first**. (2) *What's the minimum viable demo / the reusable-and-sells piece / the platform bones?* → they all collapse to one **secure extraction engine** (which already existed as the archived Terminus design). (3) *What does the product actually look like?* → specced the demo screens. Ended by capturing everything into durable docs.

## What Emerged

- **Stauss is a channel, not the product.** Losing him changes only distribution-of-first-customer; the product and captured domain knowledge are unchanged. Broker-first is the motion that makes him optional — chosen for speed *and* dependency-removal.
- **Pre-qual beats sourcing as the wedge.** Two operators described the pre-qual unprompted; sourcing is the dream Stauss kept steering toward and the weakest part of the hand at the broker tier.
- **Three build questions = one component** — the secure `extract` engine (demo + cross-vertical closer + platform bone), with compliance/PII *inside* it. Simon re-derived his own archived Terminus design; the pivot adds exactly one new bone, `match` (EF-specific).
- **The mock is dead — but so is "send me your book."** Resolution (the correction that sharpened the whole demo): run on **anonymized-from-real demo deals** (texture preserved), with the **live-drop as a post-initial-call closer**. The hero is the **Deal Detail screen**; the demo runs end-to-end through a **package draft** even where parts are faked.
- **The decouple insight:** because the first demo runs on deals we control, the **first end-to-end demo can ship fully faked** — so Phase-0 validation/selling isn't blocked on any engine build. The first piece made *real* is extraction, gated by the *closer*, not the first demo. Selling and building can start in parallel.
- **Two interactions carry disproportionate weight:** *provenance* (source-linked fields = the "it really read the doc" signature) and *override* (the literal mechanism behind "leave it 70%-moldable" + the future rubric-calibration loop).

## Produced (durable artifacts)

- Updated **Terminus** engine doc (un-archived to `active/`): anchor → EF broker-first, added the `match` module.
- **Broker-first GTM gameplan** (`active/`, for Alek): the motion, validation map, pricing, Stauss's role, demo.
- **Quintel build + GTM roadmap** (`product/quintel/`): canonical, supersedes the 2026-06-02 build-architecture doc.
- **Quintel demo shape** (`product/quintel/`): screens (Pipeline / Deal Detail hero / Package), storyboard, real-vs-faked build order — fleshed to build-ready depth.

## Open Threads

- Cold reachability (the riskiest assumption) still untested — Phase-0 outreach tests it for ~zero cost.
- Verdict taxonomy: proposed **band + 0–10 score** together; Simon to confirm.
- Pipeline screen and the live-drop flow are still wireframe-only depth.
- Deferred by choice: the be-your-own-customer / seed-the-graph move, and the first-lender (credit-flywheel) fork.
- Tension to watch: EF is the engine's anchor, but AICAP is the one paying — extraction is zero-regret either way, so it doesn't gate the build.

---

*Session note created by Bean.*
