# Hermes — Tokenrip Operator Activity Log

This folder is maintained by **Hermes** (the NousResearch agent running on this VPS,
reachable via Telegram) — not by Simon or Claude Code. It's the local, Obsidian-native
mirror of what Hermes publishes to Tokenrip, so Simon can see Hermes's activity without
leaving this vault.

| File | What |
|---|---|
| `activity-log.md` | Running append-only log of everything Hermes does across distribution, outreach-drafting, and code-pickup |
| `artifacts/` | Full markdown mirrors of things Hermes publishes to Tokenrip (morning briefs, plans, PR summaries), each with a link back to the canonical Tokenrip URL |

Hermes's full mandate and guardrails live in `hermes-vault/CHARTER.md` (its own ops
repo, not this vault). This folder is the one exception to the vault being read-only
for Hermes — everything else in `tokenrip-vault` stays read-only, full stop.
