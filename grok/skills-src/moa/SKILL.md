---
name: moa
description: "Build your own mounted agent (MOA). Pressure-tested before publish. Bootloader that fetches live Tokenrip imprint."
argument-hint: "[session context]"
allowed-tools: "Bash(npm install -g @tokenrip/cli), Bash(rip:*), Bash(curl:*)"
---

# Moa

Thin Grok adapter (bootloader).

Read `.claude/commands/moa.md` or use the full definition in `.agents/skills/moa/SKILL.md` as reference. Follow the fetch-imprint + brain artifact contract.
