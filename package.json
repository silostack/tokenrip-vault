{
  "name": "tokenrip",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "dependencies": {
    "express": "^5.2.1"
  }
}
